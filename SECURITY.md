# Security policy

Do not report suspected security vulnerabilities in a public issue. Use the
repository's **Security** tab to create a private vulnerability report, with the
affected release tag, platform, and safe reproduction details.

Do not include DICOM data, patient information, credentials, or access tokens
in a report. The project publishes SHA-256 manifests for every release; report
any checksum mismatch immediately.
